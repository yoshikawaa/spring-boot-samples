<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.github.yoshikawaa.sample.todo.domain.TodoMapper">

    <select id="findById" parameterType="String" resultType="Todo">
        SELECT
            todo_id,
            todo_title,
            finished,
            created_at
        FROM
            todo
        WHERE
            todo_id = #{todoId}
    </select>

    <select id="findAll" resultType="Todo">
        SELECT
            todo_id,
            todo_title,
            finished,
            created_at
        FROM
            todo
    </select>

    <insert id="insert" parameterType="Todo" useGeneratedKeys="true" keyProperty="todoId">
        INSERT INTO todo
        (
            todo_title,
            finished,
            created_at
        )
        VALUES
        (
            #{todoTitle},
            #{finished},
            #{createdAt}
        )
    </insert>

    <update id="update" parameterType="Todo">
        UPDATE todo
        SET
            todo_title = #{todoTitle},
            finished = #{finished},
            created_at = #{createdAt}
        WHERE
            todo_id = #{todoId}
    </update>

    <delete id="delete" parameterType="Todo">
        DELETE FROM
            todo
        WHERE
            todo_id = #{todoId}
    </delete>

    <select id="count" resultType="Long">
        SELECT
            COUNT(*)
        FROM
            todo
        WHERE
            finished = 'FALSE'
    </select>

</mapper>

